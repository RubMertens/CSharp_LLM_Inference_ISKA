what will we do
tokens to text text weghalen


definitie vector op slides toevoegen

tokenizer, mathc met een echte woord then highlight the special tokens

    generating tokens loop, add accent to the endof sequence token at the end

predict next token - label with size of vector  only when accenting Vector last


attention flow voor de code zetten

splitten van singlehead attention tussen lookto>lookingfrom en causily slide

softmax slide with split returning to the singleheadselfattention

after return to forward function before going back to the poisitional embedding slides

2d rotation slide, the init state is needs to be more correct, now the other balls jump around when first interacting with the graph.
Also we need to see the frequency shift somewhere (the applied multiplies)


rope in code also highlight the first for loop along with the second for loop
next accent should highlight the x1 x2 with the yellow

more layers remove subtitle (yellow bit)

for the layered weight slide, remove the summary comments from the code only keep the debugging blocks
accent the key, value query projection then ighlight the other projects (gate, up and down)

Then return to the forward loop and accent through the rest of the loop. (Not the version with the gates yet)


swiglu activation slide volledig uitschrijven op de slide

adding feed forward block code slide - remove adding effect after adding . step through line by line 

Remove from title slide " One head scores one kind of relationship — and a sentence has more than one"
    
multi head attention move the  " Each head learns different patterns
syntax, semantics, position, co-reference ..." to the top and the graph the the bottom and switch around the order of visibility

remove kv cache reference from Group Query Attention slide

scale slide needs to move to the end of the presentation, not before the kv cache


for slide decodenext - matrix -> vector : GroupedQueryAttentionWithCache is the last accent before next slide (going into the function)

